<!-- Charger le fichier de configuration global -->
<script src="<%= BASE_PATH %>/scripts/config.js"></script>

<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top border-bottom shadow bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="<%= BASE_PATH %>/">La plateforme SKRID</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item <%= currentPage === 'home' ? 'active' : '' %>">
                    <a class="nav-link" href="<%= BASE_PATH %>/">SKRID</a>
                </li>
                <li class="nav-item <%= currentPage === 'collections' ? 'active' : '' %>">
                    <a class="nav-link" href="<%= BASE_PATH %>/collections">Les collections</a>
                </li>
                <li class="nav-item <%= currentPage === 'interface' ? 'active' : '' %>">
                    <a class="nav-link" href="<%= BASE_PATH %>/searchInterface">Rechercher un motif</a>
                </li>
                <li class="nav-item <%= currentPage === 'manual_query' ? 'active' : '' %>">
                    <a class="nav-link" href="<%= BASE_PATH %>/manualQuery">Ecrire une requête</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

